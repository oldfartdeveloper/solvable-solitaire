module Game.Foundation where

import Data.List (List, (:), singleton)
-- import Data.List.Types (Nil)
-- import Game.Card (Card)

-- type Foundation = List (List a)

type Depth = Int

-- mkFoundation :: Foundation
-- mkFoundation = singleton (( Nil ) : ( Nil ) : Nil )
